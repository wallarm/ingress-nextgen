{{- if .Values.config.wallarm.enabled -}}
{{- include "nginx-ingress.wallarm.metricsService" (dict "metricsConfig" .Values.controller.wallarm.metrics "serviceSuffix" "wallarm" "componentSelector" "controller" "context" .) -}}
{{- end -}}
