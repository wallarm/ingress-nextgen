apiVersion: k8s.nginx.org/v1
kind: Policy
metadata:
  name: external-auth-policy-tls
spec:
  externalAuth:
    authURI: "/auth"
    authServiceName: "external-auth-svc"
    sslEnabled: true
    sslVerify: true
    sniName: "wrong-name.example.com"
    trustedCertSecret: "external-auth-ca-secret"
