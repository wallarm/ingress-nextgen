NGINX Ingress Controller {{ .Chart.Version }} (Wallarm AIO {{ .Chart.AppVersion }}) has been installed.

For release notes, see: https://docs.wallarm.com/updating-migrating/node-artifact-versions/
(upstream: https://docs.nginx.com/nginx-ingress-controller/changelog/)

For Helm installation instructions, see: https://docs.wallarm.com/7.x/admin-en/installation-kubernetes-en/

{{ if .Release.IsUpgrade -}}
If you are upgrading from a version of the chart that uses older Custom Resource Definitions (CRD) it is necessary to manually upgrade the CRDs as this is not managed by Helm.
To update to the latest version of the CRDs:
  $ kubectl apply -f https://raw.githubusercontent.com/wallarm/ingress-nextgen/{{ .Chart.Version }}/deploy/crds.yaml
{{- end -}}
