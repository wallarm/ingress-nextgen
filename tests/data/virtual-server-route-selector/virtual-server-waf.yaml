apiVersion: k8s.nginx.org/v1
kind: VirtualServer
metadata:
  name: virtual-server-route
spec:
  host: virtual-server-route.example.com
  routes:
  - path: /
    routeSelector:
      matchLabels:
       app: backends
    policies:
      - name: waf-policy
        namespace: backends
