apiVersion: k8s.nginx.org/v1
kind: Policy
metadata:
  name: external-auth-policy-custom-port
spec:
  externalAuth:
    authURI: "/auth"
    authServiceName: "external-auth-svc"
    authServicePorts:
      - 8080
