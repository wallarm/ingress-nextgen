{{- if .Values.config.wallarm.enabled -}}
{{- include "nginx-ingress.wallarm.serviceMonitor" (dict "metricsConfig" .Values.postanalytics.wd.metrics "monitorSuffix" "wallarm-wd-post" "componentSelector" (include "nginx-ingress.wallarm.postanalyticsName" .) "context" .) -}}
{{- end -}}
