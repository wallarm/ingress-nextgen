apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.org/mergeable-ingress-type: "master"
    nginx.org/policies: "external-auth-policy-valid"
  name: ext-auth-ingress-master
spec:
  ingressClassName: nginx
  rules:
  - host: ext-auth-ingress.example.com
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ext-auth-ingress-minion
  annotations:
    nginx.org/mergeable-ingress-type: "minion"
spec:
  ingressClassName: nginx
  rules:
  - host: ext-auth-ingress.example.com
    http:
      paths:
      - path: /backend1
        pathType: Prefix
        backend:
          service:
            name: backend1-svc
            port:
              number: 80
